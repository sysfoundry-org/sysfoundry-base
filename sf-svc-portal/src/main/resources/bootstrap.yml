spring:
  flyway:
    enabled: false
  application:
    name: ${sf.solution.name:example}
  cloud:
    consul:
      config:
        format: YAML
        enabled: true
        prefix: ${sf.solution.name}/service-config